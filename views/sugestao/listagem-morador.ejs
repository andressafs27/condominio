
<div class="container">
    <div class="page-inner">

      <div class="page-header d-flex justify-content-between align-items-center">
        <h3 class="fw-bold">Minhas Sugestões</h3>
        <button class="btn btn-info" onclick="registrar()">+ Sugestões</button>
      </div>
      
      <div class="row">
        <div class="col-md-12">
          <div class="card">
            <div class="card-body">
              <div class="table-responsive">
                <table
                  id="basic-datatables"
                  class="display table table-striped table-hover"
                >
                  <thead>
                    <tr>
                      <th>Data</th>
                      <th>Descrição</th>
                    </tr>
                  </thead>
                    <tbody>
                        <% for (let i=0; i < lista.length; i++){ %>
                        <tr>
                            <td><%= new Date(lista[i].data).toLocaleDateString('pt-BR') %></td>
                            <td><%= lista[i].descricao %></td>
                        </tr>
                        <% } %>
                    </tbody>
                </table>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
  
<script>
  function registrar(){
    window.location.href = '/sugestao/registrar';
  }
</script>
